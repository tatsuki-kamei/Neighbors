<% visiter = notification.visitor %>
<% item = notification.product %>

<div class="user-view clearfix ">
  <%= link_to user_path(notification.visitor) do %>
    <%= attachment_image_tag visiter, :image, format: "jpeg", fallback: "no_image.jpg", size: "50x50", class:"profile-img-circle" %>
  <% end %>
  <%= notification_form(notification) %><span class="moderate-font"><%= " (#{time_ago_in_words(notification.created_at)} 前)" %></span>
  <br>
  <% if !@comment.nil? %>
    <p class="moderate-font text-center" style="color: #C0C0C0;"><%= @messege %></p>
  <% end %>
</div>